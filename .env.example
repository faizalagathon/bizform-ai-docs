# .env.example - template untuk proyek Bizform AI Docs
# Copy file ini menjadi `.env` dan lengkapi nilai-nilainya.
# JANGAN commit file `.env` yang berisi secret ke repository publik.
# Untuk Vite, variabel yang ingin diakses di client harus diawali dengan `VITE_`.

# Supabase - wajib untuk fitur autentikasi dan database
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Contoh nilai (judul saja, jangan pakai nilai nyata di repo):
# VITE_SUPABASE_URL=https://xyzcompany.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Variabel opsional (tambahan contoh; aktifkan jika diperlukan oleh fitur lain):
# VITE_API_BASE_URL=http://localhost:5173/api
# VITE_APP_NAME=Bizform AI Docs
# VITE_SENTRY_DSN=

# Cara pakai singkat (bash):
# 1) Salin template ke .env
#    cp .env.example .env
# 2) Isi nilai yang diperlukan (.env akan dibaca saat `npm run dev` / Vite)